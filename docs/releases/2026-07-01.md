# Release — July 1, 2026

**Version:** `c06a6bc`
**Environment:** Production (courtops.app)

---

## Time Clock, Schedule & Availability — Quality-of-Life Batch

### Improvements

- **Full clock history.** The Recent Clock History list previously showed only the last 7 days. It now shows the last 30 days by default, and admins get a date-range picker in the section header to pull up any older period on demand — no more "I can't see entries before a certain date."

- **Stale swap posts cleaned up.** Open swap and take requests whose shift date has already passed no longer clutter the Shift Swap board. Past posts are hidden automatically (they'll reappear in the upcoming swap history view).

- **One way to release a schedule.** Build mode previously had two overlapping actions: "Publish drafts" (immediate, no review) and "Release" (with coverage review). The bulk Publish button is gone — releasing a window, with its coverage summary and gap warnings, is now the single path from draft to live. Individual shifts can still be published one-off from the shift detail popover.

- **Availability due dates you can't miss.** Window due dates are now urgency-colored badges with days remaining (red = due within a day, orange = within 3 days), and the availability calendar shows a banner for your most urgent unsubmitted window — it stays until you submit.

- **Clearer calendar navigation.** The back/Today/forward controls and the month label across all calendar views are larger and higher-contrast.

### Reliability fixes (same-day QA pass)

An automated QA sweep ran over the schedule, availability, swap, and time-clock paths after tonight's changes; 18 confirmed issues were fixed in a follow-up release, including:

- **Availability windows opened far in advance no longer disappear.** Windows more than 6 weeks out were invisible after creation — staff couldn't see or submit to them. All upcoming windows now show immediately.
- **Time-off request dates displayed one day early** in US timezones — fixed.
- **Hours Summary accuracy** — evening clock-ins near the start/end of a pay period were attributed to the wrong period, and the average-per-day divided by one day too few. Payroll-facing totals now tie out.
- **Swap approvals are race-safe.** Approving, denying, or cancelling a swap that changed in the meantime (e.g. the staffer cancelled their claim) now tells you to refresh instead of silently reassigning the shift or reporting a success that didn't happen.
- **Drag-to-select availability now works on phones** — previously each 30-minute slot had to be tapped individually.
- **Shared links to the swap board no longer freeze the tab bar** on the Staff page.
- Published magic-schedule shifts no longer show the internal "Magic-scheduled draft" note to staff.

### Under the hood

- New CourtOps brand assets and a formalized design-token palette (no visual changes — groundwork for consistent styling and future per-club branding).
- Payroll time-clock export design finalized; question list sent to the payroll provider (ATY) to confirm the Phoenix import format.

---

## Files Changed

- `src/app/(dashboard)/staff/page.tsx` — clock history range, stale-swap filter
- `src/app/(dashboard)/staff/tabs/clock-tab.tsx` — admin date-range history loader
- `src/app/(dashboard)/staff/tabs/schedule-tab.tsx` — bulk Publish removal
- `src/app/(dashboard)/staff/tabs/availability-windows-strip.tsx` — due-date urgency badges
- `src/app/(dashboard)/staff/tabs/availability-by-date.tsx` — due-date banner
- `src/components/calendar-month-grid.tsx` — prominent nav controls
- `src/app/globals.css`, `docs/BRAND.md`, `public/courtops-*.svg` — brand foundation
